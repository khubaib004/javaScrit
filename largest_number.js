let a = parseInt(prompt("Enter the first number : "));
let b = parseInt(prompt("Enter the first number : "));
let c = parseInt(prompt("Enter the first number : "));

if (a>b && a>c)
    console.log("A is a largest number");

else if (b>a && b>c)
    console.log("B is a largest number");

else
    console.log("C is a lagest number")
